# installation

Conda enviroment for julia simulations:

```bash
$ conda create -n ocean julia
```

Setup conda enviroment:

```bash
$ conda activate ocean && julia
julia> ]
(@ocean) pkg> add Oceananigans CairoMakie
```
